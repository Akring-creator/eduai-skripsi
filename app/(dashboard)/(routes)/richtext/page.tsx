'use client';
import { Editor } from '@/components/editor';

const RichText = () => {
  return (
    <>
      <div>
        <Editor value="Something" onChange={() => {}} />
      </div>
    </>
  );
};

export default RichText;
